buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.9.+'
        classpath files('ribot-android-gradle-0.2.jar')
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}